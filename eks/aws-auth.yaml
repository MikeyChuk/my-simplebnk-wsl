apiVersion: v1
kind: ConfigMap
metadata:
  name: aws-auth
  namespace: kube-system

data:
  mapUsers: |
    - userarn: arn:aws:iam::303974373405:user/admin
      username: admin
      groups:
        - system:masters